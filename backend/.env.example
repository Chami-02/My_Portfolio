NODE_ENV=production
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/?appName=<app-name>
JWT_SECRET=replace_with_64_char_random_string
JWT_EXPIRES_IN=7d
# PORT is only used in local development (server.js) — not needed on Vercel